# Origin of the BlogOps FastAPI backend. No trailing slash and no /v1 —
# the API client appends the version prefix itself.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# IANA time zone the UI renders timestamps in. The backend always stores UTC.
NEXT_PUBLIC_DISPLAY_TIME_ZONE=Asia/Seoul
